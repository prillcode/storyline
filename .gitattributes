# Line ending normalization

# Shell scripts should always have LF endings (Unix-style)
*.sh text eol=lf
install.sh text eol=lf
remote-install.sh text eol=lf

# PowerShell scripts should always have CRLF endings (Windows-style)
# Using -text to store with CRLF in repository (not normalized to LF)
*.ps1 -text
install.ps1 -text
remote-install.ps1 -text
uninstall.ps1 -text

# Markdown and YAML files use LF
*.md text eol=lf
*.yaml text eol=lf
*.yml text eol=lf

# Let Git handle these automatically
* text=auto
